{
  "name": "moneyflow",
  "version": "1.0.0",
  "private": true,
  "description": "Static personal finance calendar using Supabase for auth and sync.",
  "scripts": {
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.1"
  }
}
